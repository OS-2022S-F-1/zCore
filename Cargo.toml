[workspace]
members = [
    "drivers",
    "kernel-hal",
    "zircon-object",
    "zircon-syscall",
    "linux-object",
    "linux-syscall",
    "loader",
    "zCore",
    "rcore-fs/rcore-fs"
]

exclude = [
    "zircon-user",
    "rboot",
]

[profile.release]
lto = true
