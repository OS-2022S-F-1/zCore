[workspace]
members = [
    "drivers",
    "kernel-hal",
    "zircon-object",
    "zircon-syscall",
    "linux-object",
    "linux-syscall",
    "loader",
    "zCore",
    "sm-rust"
]

exclude = [
    "zircon-user",
    "rboot",
    "linux-user"
]

[profile.release]
lto = true

